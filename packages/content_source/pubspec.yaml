name: content_source
description: Content source contracts and course draft compiler for VerbaSeed.
publish_to: none
version: 0.2.1

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  course_schema:
    path: ../course_schema
  http: ^1.6.0

dev_dependencies:
  test: ^1.32.0
