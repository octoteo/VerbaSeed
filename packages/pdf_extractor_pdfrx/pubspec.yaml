name: pdf_extractor_pdfrx
description: Offline cross-platform PDF text and layout extraction adapter for VerbaSeed.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.13.0 <4.0.0'
  flutter: '>=3.47.0'

dependencies:
  flutter:
    sdk: flutter
  document_processing:
    path: ../document_processing
  pdfrx: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
