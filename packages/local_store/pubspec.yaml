name: local_store
description: Local-first persistence for VerbaSeed learner data.
publish_to: none
version: 0.2.0

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  drift: 2.35.0
  drift_flutter: 0.3.1
  uuid: 4.6.0
  content_source:
    path: ../content_source
  learning_engine:
    path: ../learning_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: 2.35.0
  build_runner: 2.16.1
